array.forEach((val) => {
//used to iterate through each value of an array

if (!args.includes(val)) {
//if the arguments are NOT included then do something

newArray.push(val);
//push values to the new array

//destructuring    if (min > max) [min, max] = [max, min];

//multiple if statements can most likely be cleaned up EX.

/*
    if (year % 4 === 0) {
        if (year % 400 === 0) {
            return true;
        } else if (!(year % 100 === 0)) {
            return true;
        } else {
            return false;
        }
    }
    return false;

    TO:   return year % 4 === 0 && (year % 100 !== 0 || year % 400 === 0);
*/

// 6.54332.toFixed(3) to round to to nearest 3 decimal place Use Number(6.544332.toFixed(3)) as toFixed returns string on its own
